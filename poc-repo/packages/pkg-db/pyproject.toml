[project]
name = "pkg-db"
version = "0.1.0"
description = "共有コード（DB層想定）。検証ではimportが通ることだけ確認する"
requires-python = ">=3.11"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/pkg_db"]
